#pragma once

// Keep the public runtime include stable while sharing the single canonical
// FrameArena implementation used by the rest of the codebase.
#include "tachyon/runtime/memory/frame_arena.h"
