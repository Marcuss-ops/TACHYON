include_guard(GLOBAL)

if(TACHYON_ENABLE_3D)
    if(TACHYON_ENABLE_FILAMENT)
        message(STATUS "[Tachyon] 3D feature enabled; Filament backend will be wired separately")
    else()
        message(STATUS "[Tachyon] 3D feature enabled; no concrete 3D backend selected yet")
    endif()
else()
    message(STATUS "[Tachyon] 3D feature disabled")
endif()
